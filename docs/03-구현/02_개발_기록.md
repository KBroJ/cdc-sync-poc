# 개발 기록

> **목표**: 개발 과정에서 발생한 문제점과 해결 과정을 기록합니다.

## 1. 프로젝트 개요

### 목표
- Oracle 양방향 CDC(Change Data Capture) 동기화 PoC
- ASIS DB(기존 시스템) ↔ TOBE DB(신규 시스템) 실시간 데이터 동기화

### 기술 스택
| 구성요소 | 기술 |
|---------|------|
| Database | Oracle XE 21c × 2 (Docker) |
| CDC | Debezium 2.4 (LogMiner 방식) |
| Message Broker | Apache Kafka |
| Sync Service | Java 17 + Spring Boot 3.2 |
| Container | Docker Compose |

### 아키텍처
```
ASIS Oracle (1521)                          TOBE Oracle (1522)
     │                                            │
     ▼                                            ▼
Debezium Connector ──► Kafka ◄── Debezium Connector
                         │
                         ▼
                   Sync Service (8082)
                         │
            ┌────────────┴────────────┐
            ▼                         ▼
     CDC_TOBE_BOOK              CDC_ASIS_BOOK
     (TOBE DB)                  (ASIS DB)
```

---

## 2. 문제점 및 해결 과정

### 2.1 UPDATE 테스트 반복 실행 시 실패

#### 문제 현상
- 대시보드에서 UPDATE 테스트 첫 번째는 성공
- 두 번째부터 계속 실패
- 에러: `ORA-17268: Year value is out of range`

#### 원인 분석
Debezium이 Oracle DATE/TIMESTAMP를 전송할 때 여러 형식 사용:
- `io.debezium.time.MicroTimestamp`: **마이크로초** (10^15 단위)
- `io.debezium.time.Timestamp`: 밀리초 (10^12 단위)
- `io.debezium.time.Date`: epoch days (10^4 단위)

기존 코드는 모든 값을 밀리초로 가정하고 처리:
```java
// 문제 코드
return new Timestamp(epochValue);  // 마이크로초 값이 들어오면 오류
```

마이크로초 값(예: `1768279886846975`)을 밀리초로 처리하면 연도가 `+58000년`이 되어 Oracle에서 오류 발생.

#### 해결 방법
값의 크기로 형식을 자동 판별하는 로직 추가:

```java
private Timestamp convertEpochToTimestamp(long epochValue) {
    long epochMs;

    if (epochValue > 100_000_000_000_000L) {
        // 마이크로초 (10^15 이상) -> 밀리초로 변환
        epochMs = epochValue / 1000;
    } else if (epochValue < 100_000) {
        // 일수 (10^5 미만) -> 밀리초로 변환
        epochMs = epochValue * 86400000L;
    } else {
        // 이미 밀리초
        epochMs = epochValue;
    }

    return new Timestamp(epochMs);
}
```

#### 파일 위치
`sync-service-java/src/main/java/com/cdc/sync/service/CdcSyncService.java`

---

### 2.2 전송된 데이터가 짤려서 표시됨

#### 문제 현상
- 대시보드 "전송된 데이터" 컬럼에서 데이터가 중간에 `...`으로 짤림
- 전체 내용 확인 불가

#### 원인 분석
`createDataPreview()` 메서드에서 의도적으로 데이터 축약:
```java
// 문제 코드
if (str.length() > 30) {
    valueStr = str.substring(0, 30) + "...";
}
if (count >= 5) {
    sb.append(", ...");
    break;  // 5개 필드만 표시
}
```

#### 해결 방법
전체 데이터 표시하도록 수정:
```java
private String createDataPreview(Map<String, Object> data) {
    StringBuilder sb = new StringBuilder();
    sb.append("{\n");
    int count = 0;
    for (Map.Entry<String, Object> entry : data.entrySet()) {
        if (count > 0) sb.append(",\n");
        String key = entry.getKey();
        Object value = entry.getValue();

        // 값 포맷팅 (전체 표시, 짤림 없음)
        String valueStr = (value instanceof String)
            ? "\"" + value + "\""
            : String.valueOf(value);

        sb.append("  ").append(key).append(": ").append(valueStr);
        count++;
        // break 제거 - 모든 필드 표시
    }
    sb.append("\n}");
    return sb.toString();
}
```

UI에서는 클릭하면 펼쳐지는 방식으로 처리:
```css
.data-cell.collapsed {
    max-height: 60px;
    overflow: hidden;
}
.data-cell.expanded {
    max-height: none;
}
```

---

### 2.3 테이블 스크롤 시 헤더도 같이 스크롤됨

#### 문제 현상
- 테이블 데이터가 많아서 스크롤하면 컬럼명(헤더)이 사라짐
- 어떤 컬럼인지 확인 불가

#### 해결 방법
CSS `position: sticky` 적용:
```css
.table-wrapper {
    max-height: 250px;
    overflow-y: auto;
}

th {
    position: sticky;
    top: 0;
    background: #0d0d1a;
    z-index: 10;
}
```

---

### 2.4 HTML이 Java 파일 안에 포함됨

#### 문제 현상
`DashboardController.java`에서 HTML을 문자열로 반환:
```java
@GetMapping("/dashboard")
@ResponseBody
public String dashboard() {
    return """
    <!DOCTYPE html>
    <html>
    ... 수백 줄의 HTML ...
    </html>
    """;
}
```

#### 문제점
- 유지보수 어려움 (HTML 수정하려면 Java 파일 수정 필요)
- IDE에서 HTML 문법 하이라이팅 불가
- 프론트엔드 개발자 협업 어려움
- 빌드 없이 HTML 수정 불가

#### 해결 방법 (1차 - 정적 파일 분리)
`src/main/resources/static/dashboard.html`로 분리:
```java
@GetMapping("/dashboard")
public String dashboard() {
    return "redirect:/dashboard.html";
}
```

#### 개선 방향 (2차 - Thymeleaf 적용 예정)
Spring Boot 표준 템플릿 엔진 Thymeleaf 사용:
- 동적 데이터 바인딩 가능
- 서버사이드 렌더링
- `src/main/resources/templates/` 사용

---

### 2.5 Java 주석에 HTML 태그 포함

#### 문제 현상
Javadoc 주석에 HTML 태그 사용:
```java
/**
 * <p>설명입니다.</p>
 * <ul>
 *   <li>항목1</li>
 *   <li>항목2</li>
 * </ul>
 */
```

#### 문제점
- 코드 가독성 저하
- IDE에서 주석 읽기 불편

#### 해결 방법
일반 텍스트 형식으로 변경:
```java
/**
 * 설명입니다.
 *
 * 항목:
 *   - 항목1
 *   - 항목2
 */
```

#### 수정한 파일 목록
- `DataSourceConfig.java`
- `KafkaConfig.java`
- `CdcEvent.java`
- `CdcKafkaConsumer.java`
- `CdcMonitoringService.java`
- `MonitoringController.java`
- `CdcSyncService.java`
- `TestController.java`

---

### 2.6 CDC 테이블에만 INSERT되고 실제 테이블 동기화 안됨

#### 문제 현상
- ASIS BOOK_INFO 변경 → CDC_TOBE_BOOK에 INSERT됨
- 하지만 TB_BOOK은 변경 안됨
- 대시보드에서 ASIS와 TOBE의 BOOK_TITLE이 다르게 표시

#### 원인 분석
현재 CDC 흐름:
```
ASIS BOOK_INFO 변경
    → Debezium 감지
    → Kafka 전송
    → Sync Service 수신
    → CDC_TOBE_BOOK INSERT (여기까지만 구현)
    → TB_BOOK 반영 (미구현!)
```

CDC 테이블은 "이벤트 저장소"일 뿐, 실제 테이블 동기화는 별도 구현 필요.

#### 해결 방향 (진행 예정)
1. CDC 이벤트 수신 시 실제 테이블에 CUD 수행
2. 그 후 CDC 테이블에 기록 (로깅용)
3. 스키마 매핑 적용 (BOOK_TITLE ↔ TITLE 등)

올바른 순서:
```
CDC 이벤트 수신
    → 실제 테이블 CUD (TB_BOOK INSERT/UPDATE/DELETE)
    → CDC 테이블 기록 (CDC_TOBE_BOOK INSERT)
```

---

## 3. 스키마 매핑

ASIS와 TOBE의 스키마가 다르므로 변환 필요:

| ASIS (BOOK_INFO) | TOBE (TB_BOOK) | 변환 |
|------------------|----------------|------|
| BOOK_ID | BOOK_ID | 동일 |
| BOOK_TITLE | TITLE | 컬럼명 변경 |
| AUTHOR | AUTHOR_NAME | 컬럼명 변경 |
| CATEGORY | CATEGORY_CD | 컬럼명 변경 |
| STATUS (Y/N) | IS_ACTIVE (1/0) | 값 변환 |
| REG_DATE | CREATED_AT | 컬럼명 변경 |
| MOD_DATE | UPDATED_AT | 컬럼명 변경 |

---

## 4. 테스트 API 추가

### 추가된 엔드포인트

| Method | URL | 설명 |
|--------|-----|------|
| POST | /api/test/asis/insert | ASIS DB INSERT 테스트 |
| POST | /api/test/asis/update | ASIS DB UPDATE 테스트 |
| POST | /api/test/asis/delete | ASIS DB DELETE 테스트 |
| POST | /api/test/tobe/insert | TOBE DB INSERT 테스트 |
| POST | /api/test/tobe/update | TOBE DB UPDATE 테스트 |
| POST | /api/test/tobe/delete | TOBE DB DELETE 테스트 |
| GET | /api/test/asis/data | ASIS 원본 테이블 조회 |
| GET | /api/test/tobe/data | TOBE 원본 테이블 조회 |
| GET | /api/test/asis/cdc | ASIS CDC 테이블 조회 |
| GET | /api/test/tobe/cdc | TOBE CDC 테이블 조회 |

### 응답 예시 (UPDATE)
```json
{
  "success": true,
  "operation": "UPDATE",
  "message": "ASIS DB UPDATE 완료",
  "sql": "UPDATE BOOK_INFO SET BOOK_TITLE = ?, MOD_DATE = SYSDATE WHERE BOOK_ID = ?",
  "beforeData": {
    "BOOK_ID": 1,
    "BOOK_TITLE": "기존제목",
    "AUTHOR": "저자명"
  },
  "afterData": {
    "BOOK_ID": 1,
    "BOOK_TITLE": "변경된제목",
    "AUTHOR": "저자명"
  },
  "rowsAffected": 1,
  "targetCdcTable": "CDC_TOBE_BOOK"
}
```

---

## 5. 파일 구조 (리팩토링 후)

> **업데이트**: 2026-01-14 패키지 구조 리팩토링 완료 (섹션 14 참조)

```
sync-service-java/
├── src/main/java/com/cdc/sync/
│   ├── SyncServiceApplication.java      # 메인 클래스
│   ├── config/
│   │   ├── DataSourceConfig.java        # 다중 데이터소스 설정
│   │   ├── KafkaConfig.java             # Kafka Consumer 설정
│   │   └── CdcSimulatorConfig.java      # 동적 테이블 설정
│   ├── consumer/
│   │   └── CdcKafkaConsumer.java        # Kafka 리스너
│   ├── domain/                          # (model → domain 변경)
│   │   └── CdcEvent.java                # CDC 이벤트 도메인
│   ├── service/
│   │   ├── CdcSyncService.java          # 동기화 서비스 (핵심)
│   │   └── CdcMonitoringService.java    # 인메모리 통계
│   └── controller/                      # (monitoring → controller 변경)
│       ├── DashboardController.java     # Thymeleaf 뷰
│       ├── MonitoringController.java    # 모니터링 API
│       └── SimulatorController.java     # 시뮬레이터 API
├── src/main/resources/
│   ├── application.yml                  # 설정 파일
│   └── templates/
│       └── simulator.html               # Thymeleaf 템플릿
└── pom.xml
```

---

## 6. 남은 작업

### 6.1 실제 테이블 동기화 구현
- [ ] CDC 이벤트 수신 시 실제 테이블 CUD 수행
- [ ] 스키마 매핑 적용 (ASIS ↔ TOBE 컬럼 변환)
- [ ] 무한루프 방지 로직 (해시 기반)

### 6.2 Thymeleaf 적용
- [ ] pom.xml에 의존성 추가
- [ ] templates/ 폴더로 HTML 이동
- [ ] 동적 데이터 바인딩

---

## 7. 참고 사항

### Debezium 타임스탬프 형식
| 형식 | 값 범위 | 예시 |
|------|---------|------|
| MicroTimestamp | 10^15 이상 | 1768279886846975 |
| Timestamp (ms) | 10^12 ~ 10^15 | 1768279886846 |
| Date (days) | 10^5 미만 | 19736 (2024-01-13) |

### Debezium NUMBER 형식
Oracle NUMBER는 Base64 인코딩된 BigInteger로 전송:
```json
{ "scale": 0, "value": "AQ==" }
```

디코딩 로직:
```java
byte[] bytes = Base64.getDecoder().decode("AQ==");
BigInteger bigInt = new BigInteger(bytes);  // 결과: 1
```

---

## 8. 커밋 히스토리

| 날짜 | 내용 |
|------|------|
| 2026-01-13 | 초기 PoC 환경 구축 |
| 2026-01-13 | Sync Service Python → Java 전환 |
| 2026-01-13 | 모니터링 대시보드 추가 |
| 2026-01-13 | UPDATE 반복 실패 버그 수정 (타임스탬프) |
| 2026-01-13 | INSERT/UPDATE/DELETE 테스트 API 추가 |
| 2026-01-13 | HTML 분리, 주석 정리 |
| 2026-01-13 | Oracle Scheduler Job 추가 (TOBE/ASIS 양쪽) |
| 2026-01-13 | Docker init 스크립트 수정 (PDB 전환, 스키마 전환) |
| 2026-01-13 | 프로시저 SQLERRM 오류 수정 |

---

## 9. TODO (해야 할 작업)

### 9.1 필수 작업

| 순서 | 작업 | 상태 | 설명 |
|:----:|------|:----:|------|
| 1 | WORKER 프로시저 호출 설정 | ✅ | Oracle Scheduler Job 추가 완료 |
| 2 | ASIS DB 프로시저 생성 | ✅ | 이미 존재, Scheduler Job 추가 완료 |
| 3 | Docker 초기화 스크립트 수정 | ✅ | PDB 전환, 스키마 전환, SQLERRM 수정 |
| 4 | 양방향 테스트 | 🔄 | Docker 재시작 후 테스트 필요 |

### 9.2 선택 작업 → 대시보드 개선 (완료)

| 순서 | 작업 | 상태 | 설명 |
|:----:|------|:----:|------|
| 4 | Thymeleaf 적용 | ✅ | static HTML → 템플릿 엔진 |
| 5 | 대시보드 개선 | ✅ | CDC 동기화 시뮬레이터로 전환 |

> **상세 계획**: `17_대시보드_개선_계획.md` 참조

### 9.3 작업 상세

#### 1. WORKER 프로시저 호출 설정

**문제**: `SP_WORKER_BOOK` 프로시저가 존재하지만 호출되지 않음

**해결 방안** (택1):
- **방안 A**: Oracle Scheduler 설정 (주기적 실행)
- **방안 B**: Java Sync Service에서 프로시저 호출 추가
- **방안 C**: CDC 테이블 INSERT 트리거로 자동 호출

**관련 파일**:
- `poc/tobe-oracle/init/04_create_procedures.sql` - 프로시저 정의
- `sync-service-java/.../CdcSyncService.java` - CDC 테이블 INSERT

#### 2. ASIS DB 프로시저 생성

TOBE DB에만 프로시저가 있음. ASIS DB에도 동일하게 필요:
- `SP_WORKER_BOOK` (CDC_ASIS_BOOK → BOOK_INFO)
- `FN_CONVERT_CODE` (코드 역변환)
- `FN_IS_LOOP`, `FN_GENERATE_HASH`

**생성 위치**: `poc/asis-oracle/init/04_create_procedures.sql`

---

## 10. Oracle Scheduler 설정 (2026-01-13)

### 10.1 문제점
- WORKER 프로시저가 존재하지만 아무도 호출하지 않아 동기화 안 됨
- CDC_TOBE_BOOK에 데이터는 들어가지만 TB_BOOK에 반영 안 됨

### 10.2 해결: Oracle Scheduler Job 추가

```sql
-- 5초마다 WORKER 프로시저 자동 실행
BEGIN
    DBMS_SCHEDULER.CREATE_JOB(
        job_name        => 'JOB_CDC_WORKER',
        job_type        => 'PLSQL_BLOCK',
        job_action      => 'BEGIN SP_RUN_ALL_WORKERS; END;',
        start_date      => SYSTIMESTAMP,
        repeat_interval => 'FREQ=SECONDLY;INTERVAL=5',
        enabled         => TRUE
    );
END;
```

### 10.3 Docker init 스크립트 수정사항

1. **00_setup_user.sql 추가**: 사용자 생성 및 권한 부여
2. **PDB 전환 추가**: `ALTER SESSION SET CONTAINER = XEPDB1;`
3. **스키마 전환 추가**: `ALTER SESSION SET CURRENT_SCHEMA = TOBE_USER;`
4. **SQLERRM 오류 수정**: `SUBSTR(SQLERRM, 1, 500)` 사용

### 10.4 수정된 파일 목록
- `poc/tobe-oracle/init/00_setup_user.sql` (신규)
- `poc/tobe-oracle/init/01~05_*.sql` (PDB/스키마 전환 추가)
- `poc/tobe-oracle/init/04_create_procedures.sql` (Scheduler Job, SQLERRM 수정)
- `poc/asis-oracle/init/00_setup_user.sql` (신규)
- `poc/asis-oracle/init/01~05_*.sql` (PDB/스키마 전환 추가)
- `poc/asis-oracle/init/04_create_procedures.sql` (Scheduler Job, SQLERRM 수정)

### 10.5 다음 작업
- Docker 재시작 후 테이블/프로시저 생성 확인
- Scheduler Job 실행 상태 확인
- CDC 전체 흐름 테스트 (ASIS → TOBE → TB_BOOK 동기화)

---

## 11. Debezium 설정 및 전체 CDC 흐름 테스트 (2026-01-13)

### 11.1 Docker 환경 시작 - 포트 충돌 문제

#### 문제 현상
```
Error response from daemon: ports are not available: exposing port TCP 0.0.0.0:1521 -> 127.0.0.1:0:
listen tcp 0.0.0.0:1521: bind: An attempt was made to access a socket in a way forbidden by its access permissions.
```

Docker Desktop 재시작 후에도 1521, 1522 포트 사용 불가.

#### 원인 분석
Windows Hyper-V 또는 NAT 서비스가 특정 포트 범위를 예약하여 사용 불가능.
`netsh interface ipv4 show excludedportrange protocol=tcp` 명령으로 확인 가능.

#### 해결 방법
docker-compose.yml의 포트를 높은 번호로 변경:
```yaml
# 변경 전
asis-oracle:
  ports:
    - "1521:1521"
tobe-oracle:
  ports:
    - "1522:1521"

# 변경 후
asis-oracle:
  ports:
    - "15210:1521"
tobe-oracle:
  ports:
    - "15220:1521"
```

#### 수정 파일
- `poc/docker-compose.yml`

---

### 11.2 Debezium 설정 - ARCHIVELOG 모드 활성화

#### 문제 현상
Debezium Oracle LogMiner 커넥터는 ARCHIVELOG 모드 필수.
Oracle XE 기본 설정은 NOARCHIVELOG 모드.

#### 확인 방법
```sql
SELECT LOG_MODE FROM V$DATABASE;
-- 결과: NOARCHIVELOG
```

#### 해결 방법
Oracle을 MOUNT 상태에서 ARCHIVELOG 모드로 변경:

```bash
docker exec asis-oracle bash -c 'export ORACLE_SID=XE && sqlplus -s / as sysdba << EOF
SHUTDOWN IMMEDIATE;
STARTUP MOUNT;
ALTER DATABASE ARCHIVELOG;
ALTER DATABASE OPEN;
ALTER PLUGGABLE DATABASE ALL OPEN;
exit;
EOF'
```

#### 적용 대상
- ASIS Oracle (asis-oracle 컨테이너)
- TOBE Oracle (tobe-oracle 컨테이너)

---

### 11.3 Debezium 설정 - c##dbzuser 생성

#### 필요성
Debezium Oracle 커넥터가 LogMiner를 사용하려면 특별한 권한을 가진 Common User 필요.

#### 생성 스크립트
```sql
-- CDB 레벨에서 실행 (SYSDBA로)
CREATE USER c##dbzuser IDENTIFIED BY dbz
  DEFAULT TABLESPACE USERS
  QUOTA UNLIMITED ON USERS
  CONTAINER=ALL;

-- 필수 권한 부여
GRANT CREATE SESSION TO c##dbzuser CONTAINER=ALL;
GRANT SET CONTAINER TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$DATABASE TO c##dbzuser CONTAINER=ALL;
GRANT FLASHBACK ANY TABLE TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ANY TABLE TO c##dbzuser CONTAINER=ALL;
GRANT SELECT_CATALOG_ROLE TO c##dbzuser CONTAINER=ALL;
GRANT EXECUTE_CATALOG_ROLE TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ANY TRANSACTION TO c##dbzuser CONTAINER=ALL;
GRANT LOGMINING TO c##dbzuser CONTAINER=ALL;
GRANT CREATE TABLE TO c##dbzuser CONTAINER=ALL;
GRANT LOCK ANY TABLE TO c##dbzuser CONTAINER=ALL;
GRANT CREATE SEQUENCE TO c##dbzuser CONTAINER=ALL;
GRANT EXECUTE ON DBMS_LOGMNR TO c##dbzuser CONTAINER=ALL;
GRANT EXECUTE ON DBMS_LOGMNR_D TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOG TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOG_HISTORY TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOGMNR_LOGS TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOGMNR_CONTENTS TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOGMNR_PARAMETERS TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$LOGFILE TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$ARCHIVED_LOG TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$ARCHIVE_DEST_STATUS TO c##dbzuser CONTAINER=ALL;
GRANT SELECT ON V_$TRANSACTION TO c##dbzuser CONTAINER=ALL;
```

#### Supplemental Logging 활성화
```sql
ALTER DATABASE ADD SUPPLEMENTAL LOG DATA;
ALTER DATABASE ADD SUPPLEMENTAL LOG DATA (ALL) COLUMNS;
```

---

### 11.4 Debezium 커넥터 등록 - CONTINUOUS_MINE 오류

#### 문제 현상
커넥터 등록 후 Task FAILED:
```
ORA-44609: CONTINOUS_MINE is desupported for use with DBMS_LOGMNR.START_LOGMNR.
```

#### 원인 분석
Oracle 21c에서 `CONTINUOUS_MINE` 옵션이 더 이상 지원되지 않음 (deprecated).
Debezium 설정에서 `log.mining.continuous.mine: true` 옵션 제거 필요.

#### 해결 방법
커넥터 설정에서 `log.mining.continuous.mine` 옵션 제거:

```bash
# ASIS 커넥터 등록
curl -X POST http://localhost:8083/connectors -H "Content-Type: application/json" -d '{
  "name": "asis-connector",
  "config": {
    "connector.class": "io.debezium.connector.oracle.OracleConnector",
    "tasks.max": "1",
    "database.hostname": "asis-oracle",
    "database.port": "1521",
    "database.user": "c##dbzuser",
    "database.password": "dbz",
    "database.dbname": "XE",
    "database.pdb.name": "XEPDB1",
    "topic.prefix": "asis",
    "table.include.list": "ASIS_USER.BOOK_INFO,ASIS_USER.MEMBER_INFO,ASIS_USER.LEGACY_CODE",
    "schema.history.internal.kafka.bootstrap.servers": "kafka:29092",
    "schema.history.internal.kafka.topic": "schema-changes.asis",
    "log.mining.strategy": "online_catalog"
  }
}'

# TOBE 커넥터 등록
curl -X POST http://localhost:8083/connectors -H "Content-Type: application/json" -d '{
  "name": "tobe-connector",
  "config": {
    "connector.class": "io.debezium.connector.oracle.OracleConnector",
    "tasks.max": "1",
    "database.hostname": "tobe-oracle",
    "database.port": "1521",
    "database.user": "c##dbzuser",
    "database.password": "dbz",
    "database.dbname": "XE",
    "database.pdb.name": "XEPDB1",
    "topic.prefix": "tobe",
    "table.include.list": "TOBE_USER.TB_BOOK,TOBE_USER.TB_MEMBER,TOBE_USER.TB_NEW_SERVICE",
    "schema.history.internal.kafka.bootstrap.servers": "kafka:29092",
    "schema.history.internal.kafka.topic": "schema-changes.tobe",
    "log.mining.strategy": "online_catalog"
  }
}'
```

#### 커넥터 상태 확인
```bash
curl -s http://localhost:8083/connectors/asis-connector/status
curl -s http://localhost:8083/connectors/tobe-connector/status
# 결과: {"connector":{"state":"RUNNING"}, "tasks":[{"state":"RUNNING"}]}
```

---

### 11.5 SP_WORKER_BOOK 프로시저 - SQLERRM 버그 수정

#### 문제 현상
WORKER 프로시저 호출 시 오류:
```
ORA-06550: PLS-00905: object TOBE_USER.SP_WORKER_BOOK is invalid
```

#### 원인 분석
프로시저 컴파일 오류:
```
PL/SQL: ORA-00904: "SQLERRM": invalid identifier
```

`SQLERRM`은 PL/SQL 예약어로, UPDATE 문 안에서 직접 사용 불가.

#### 문제 코드
```sql
EXCEPTION
    WHEN OTHERS THEN
        UPDATE CDC_TOBE_BOOK
        SET PROCESSED_YN = 'E',
            ERROR_MSG = SUBSTR(SQLERRM, 1, 500)  -- 오류!
        WHERE CDC_SEQ = rec.CDC_SEQ;
```

#### 해결 방법
SQLERRM을 먼저 변수에 저장 후 사용:

```sql
IS
    v_err_msg VARCHAR2(500);  -- 변수 선언
BEGIN
    ...
EXCEPTION
    WHEN OTHERS THEN
        v_err_msg := SUBSTR(SQLERRM, 1, 500);  -- 변수에 저장
        UPDATE CDC_TOBE_BOOK
        SET PROCESSED_YN = 'E',
            ERROR_MSG = v_err_msg  -- 변수 사용
        WHERE CDC_SEQ = rec.CDC_SEQ;
```

#### 수정 대상
- TOBE: `SP_WORKER_BOOK`, `SP_WORKER_MEMBER`, `SP_WORKER_LEGACY_CODE`
- ASIS: `SP_WORKER_BOOK`, `SP_WORKER_MEMBER`, `SP_WORKER_NEW_SERVICE`

---

### 11.6 CDC_ASIS_BOOK 테이블 - 컬럼 누락

#### 문제 현상
TOBE → ASIS 동기화 시 오류:
```
ORA-00904: "UPDATED_BY": invalid identifier
```

#### 원인 분석
TB_BOOK 테이블에는 `CREATED_AT`, `UPDATED_AT`, `CREATED_BY`, `UPDATED_BY` 컬럼이 있지만,
CDC_ASIS_BOOK 테이블에는 해당 컬럼이 없음.

Debezium은 원본 테이블의 모든 컬럼을 전송하고,
Sync Service는 동적 SQL로 모든 컬럼을 INSERT하려고 시도.

#### 해결 방법
CDC_ASIS_BOOK에 누락된 컬럼 추가:

```sql
ALTER TABLE CDC_ASIS_BOOK ADD (
    CREATED_AT TIMESTAMP,
    UPDATED_AT TIMESTAMP,
    CREATED_BY VARCHAR2(50),
    UPDATED_BY VARCHAR2(50)
);
```

---

### 11.7 전체 CDC 흐름 테스트 결과

#### 테스트 환경
| 구성요소 | 상태 |
|---------|:----:|
| ASIS Oracle (15210) | ✅ Running |
| TOBE Oracle (15220) | ✅ Running |
| Kafka | ✅ Running |
| Kafka Connect | ✅ Running |
| Debezium ASIS Connector | ✅ Running |
| Debezium TOBE Connector | ✅ Running |
| Sync Service (8082) | ✅ Running |

#### 테스트 시나리오 1: ASIS → TOBE

```
[테스트]
curl -X POST http://localhost:8082/api/test/asis/insert
→ ASIS BOOK_INFO에 BOOK_ID=5 INSERT

[흐름]
1. Debezium이 BOOK_INFO 변경 감지
2. Kafka asis.ASIS_USER.BOOK_INFO 토픽으로 발행
3. Sync Service가 consume
4. CDC_TOBE_BOOK에 INSERT (PROCESSED_YN='N')
5. Oracle Scheduler Job이 SP_WORKER_BOOK 호출
6. STAGING_TOBE_BOOK에 스키마 변환 후 INSERT
7. TB_BOOK에 최종 반영

[결과]
- CDC_TOBE_BOOK: PROCESSED_YN='Y' ✅
- TB_BOOK: BOOK_ID=5 존재 ✅
```

#### 테스트 시나리오 2: TOBE → ASIS

```
[테스트]
curl -X POST http://localhost:8082/api/test/tobe/insert
→ TOBE TB_BOOK에 BOOK_ID=7 INSERT

[흐름]
1. Debezium이 TB_BOOK 변경 감지
2. Kafka tobe.TOBE_USER.TB_BOOK 토픽으로 발행
3. Sync Service가 consume
4. CDC_ASIS_BOOK에 INSERT (PROCESSED_YN='N')
5. Oracle Scheduler Job이 SP_WORKER_BOOK 호출
6. STAGING_ASIS_BOOK에 스키마 변환 후 INSERT
7. BOOK_INFO에 최종 반영

[결과]
- CDC_ASIS_BOOK: PROCESSED_YN='Y' ✅
- BOOK_INFO: BOOK_ID=7 존재 ✅
```

#### 최종 데이터 상태
| DB | 테이블 | 건수 | 비고 |
|:--:|:------:|:----:|------|
| ASIS | BOOK_INFO | 6건 | 1,2,3,4,5 + 7(TOBE에서 동기화) |
| TOBE | TB_BOOK | 7건 | 1,2,3 + 4,5(ASIS에서 동기화) + 6,7 |

---

### 11.8 Docker 재시작 시 적용 필요 항목

현재 수동으로 적용한 설정들 (Docker 볼륨 삭제 시 재적용 필요):

| 항목 | 적용 위치 | 초기화 스크립트 반영 필요 |
|------|----------|:------------------------:|
| ARCHIVELOG 모드 | 양쪽 Oracle | ⚠️ 별도 처리 필요 |
| c##dbzuser 생성 | 양쪽 Oracle CDB | ⚠️ 신규 스크립트 필요 |
| Supplemental Logging | 양쪽 Oracle | ⚠️ 신규 스크립트 필요 |
| SP_WORKER_BOOK 수정 | 04_create_procedures.sql | ✅ 반영 필요 |
| CDC_ASIS_BOOK 컬럼 추가 | 02_create_cdc_tables.sql | ✅ 반영 필요 |
| docker-compose 포트 변경 | docker-compose.yml | ✅ 이미 반영됨 |

---

### 11.9 접속 정보 (현재 환경)

| 서비스 | URL/접속정보 |
|--------|-------------|
| 대시보드 | http://localhost:8082/dashboard |
| Kafka UI | http://localhost:8081 |
| Kafka Connect API | http://localhost:8083 |
| ASIS Oracle | localhost:15210 / asis_user / asis123 |
| TOBE Oracle | localhost:15220 / tobe_user / tobe123 |

---

---

## 12. CDC 동기화 시뮬레이터 개선 (2026-01-14)

### 12.1 개선 배경

기존 대시보드의 문제점:
- static HTML로 동적 데이터 표시 어려움
- BOOK 테이블만 하드코딩됨
- 테이블 추가 시 Java/HTML 수정 필요
- 동기화 흐름 추적 어려움

### 12.2 개선 내용

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| UI 엔진 | Static HTML | Thymeleaf 템플릿 |
| 테이블 지원 | BOOK 하드코딩 | YAML 설정 기반 동적 |
| API | TestController (하드코딩) | SimulatorController (동적) |
| 기능 구분 | 단일 화면 | 3개 탭 (테스트/흐름/이력) |

### 12.3 새 파일 구조

```
sync-service-java/src/main/
├── java/com/cdc/sync/
│   ├── config/
│   │   └── CdcSimulatorConfig.java    # 테이블 설정 바인딩 (NEW)
│   └── monitoring/
│       ├── SimulatorController.java    # 동적 API (NEW)
│       └── DashboardController.java    # Thymeleaf 컨트롤러 (수정)
└── resources/
    ├── application.yml                 # 테이블 설정 추가
    └── templates/
        └── simulator.html              # Thymeleaf 템플릿 (NEW)
```

### 12.4 새 API 엔드포인트 (SimulatorController)

| Method | URL | 설명 |
|--------|-----|------|
| GET | /api/simulator/{tableName}/{db}/data | 원본 테이블 조회 |
| GET | /api/simulator/{tableName}/{db}/cdc | CDC 테이블 조회 |
| GET | /api/simulator/{tableName}/{db}/staging | STAGING 테이블 조회 |
| GET | /api/simulator/sync-log | 동기화 로그 조회 |
| GET | /api/simulator/stats | 통계 조회 |
| POST | /api/simulator/{tableName}/{db}/insert | INSERT 테스트 |
| POST | /api/simulator/{tableName}/{db}/update | UPDATE 테스트 |
| POST | /api/simulator/{tableName}/{db}/delete | DELETE 테스트 |

### 12.5 테이블 설정 (application.yml)

```yaml
cdc:
  simulator:
    tables:
      - name: BOOK
        display-name: 도서
        direction: BIDIRECTIONAL
        asis:
          table: BOOK_INFO
          pk: BOOK_ID
          columns:
            - { name: BOOK_ID, type: NUMBER, display: "도서ID", required: true }
            # ...
        tobe:
          table: TB_BOOK
          pk: BOOK_ID
          columns:
            - { name: BOOK_ID, type: NUMBER, display: "도서ID", required: true }
            # ...
        cdc-table:
          asis: CDC_ASIS_BOOK
          tobe: CDC_TOBE_BOOK
        staging-table:
          asis: STAGING_ASIS_BOOK
          tobe: STAGING_TOBE_BOOK
```

### 12.6 지원 테이블 및 동기화 방향

| 테이블 | 표시명 | 방향 | ASIS 테이블 | TOBE 테이블 |
|--------|--------|------|-------------|-------------|
| BOOK | 도서 | 양방향 | BOOK_INFO | TB_BOOK |
| MEMBER | 회원 | 양방향 | MEMBER_INFO | TB_MEMBER |
| LEGACY_CODE | 레거시코드 | 구→신 | LEGACY_CODE | TB_LEGACY_CODE |
| NEW_SERVICE | 신규서비스 | 신→구 | NEW_SERVICE_RECV | TB_NEW_SERVICE |

### 12.7 UI 탭 구조

1. **테스트 실행** - CUD 테스트, 원본 테이블 조회
2. **동기화 흐름** - 원본→CDC→STAGING→대상 단계별 추적
3. **이력/통계** - CDC_SYNC_LOG 조회, 성공/실패 통계

### 12.8 접속 URL

| 페이지 | URL |
|--------|-----|
| 시뮬레이터 | http://localhost:8082/simulator |
| 시뮬레이터 (테이블 지정) | http://localhost:8082/simulator?table=MEMBER |
| 기존 URL (리다이렉트) | http://localhost:8082/dashboard → /simulator |

---

## 13. Debezium 설정 자동화 (2026-01-14)

> **발생 시점**: 2026-01-14 / Debezium 수동 설정 자동화 작업

### 13.1 배경

기존에는 Docker 환경 시작 후 Debezium 설정을 수동으로 해야 했습니다:
1. ARCHIVELOG 모드 활성화 (수동)
2. c##dbzuser 생성 및 권한 부여 (수동)
3. Supplemental Logging 활성화 (수동)
4. Debezium 커넥터 등록 (수동)

**문제점**: 볼륨 삭제 후 재시작 시마다 위 과정을 반복해야 함

### 13.2 해결 방법

**통합 시작 스크립트** 작성으로 한 번의 명령으로 모든 설정 완료:

```bash
# Linux/Mac/Git Bash
./scripts/start-all.sh

# Windows (Git Bash 필요)
scripts/start-all.bat
```

### 13.3 스크립트 구조

| 스크립트 | 역할 |
|----------|------|
| `start-all.sh` | 통합 시작 (docker-compose up + 설정 + 커넥터) |
| `setup-debezium-oracle.sh` | Oracle 설정 (ARCHIVELOG, c##dbzuser) |
| `register-connectors.sh` | Debezium 커넥터 등록 |

### 13.4 멱등성 보장

스크립트는 **이미 설정된 경우 건너뛰기** 기능이 있어 반복 실행해도 안전합니다:

```
[asis-oracle]
  현재 상태: LOG_MODE=ARCHIVELOG, c##dbzuser=1명
  ARCHIVELOG 모드: 이미 설정됨 (SKIP)
  Supplemental Logging: 완료
  c##dbzuser: 이미 존재함 (SKIP)
```

### 13.5 테스트 결과

볼륨 완전 삭제 후 `start-all.sh` 실행:

```
1단계: Docker Compose 시작          ✓
2단계: Oracle healthy 대기          ✓ (약 170초)
3단계: Debezium Oracle 설정         ✓
4단계: Debezium 커넥터 등록         ✓
CDC 동기화 테스트                   ✓ (ASIS→TOBE 정상)
```

**총 소요 시간**: 약 5분

### 13.6 핵심 포인트

> **기억하세요**: `docker-compose down -v` (볼륨 삭제) 후에만 재설정이 필요합니다.
> 일반 재시작(`docker-compose restart`)에서는 설정이 유지됩니다.

---

---

## 14. Java 소스 리팩토링 (2026-01-14)

> **발생 시점**: 2026-01-14 / 죽은 코드 정리 및 패키지 구조 개선

### 14.1 배경

폐쇄망에서 참고용으로 활용 가능하도록 코드 구조 정리 필요:
- 죽은 코드 제거 (유지보수성 향상)
- 패키지 구조 개선 (역할별 명확한 분리)
- 교육용 주석 추가 (이해도 향상)

### 14.2 죽은 코드 분석 및 삭제

#### 분석 결과

| 파일 | 문제점 | 조치 |
|------|--------|------|
| `TestController.java` | BOOK 테이블 하드코딩, SimulatorController로 대체됨 | **삭제** |
| `static/dashboard.html` | DashboardController가 /simulator로 리다이렉트, 미사용 | **삭제** |
| `MonitoringController.java` | CdcSyncService에서 사용 중 | 유지 |

#### 삭제한 파일
- `monitoring/TestController.java` (405 lines)
- `static/dashboard.html` (869 lines)
- **총 1,274 라인 제거**

### 14.3 패키지 구조 변경

#### Before → After

```
[Before]                           [After]
com.cdc.sync/                      com.cdc.sync/
├── config/                        ├── config/         ✓ 유지
├── consumer/                      ├── consumer/       ✓ 유지
├── model/          → 삭제         ├── domain/         ✓ 신규 (DDD 용어)
│   └── CdcEvent.java              │   └── CdcEvent.java
├── monitoring/     → 삭제         ├── controller/     ✓ 신규 (역할 명확화)
│   ├── Controllers...             │   ├── DashboardController.java
│   └── CdcMonitoringService.java  │   ├── MonitoringController.java
├── service/                       │   └── SimulatorController.java
│   └── CdcSyncService.java        ├── service/        ✓ 확장
└── SyncServiceApplication.java    │   ├── CdcSyncService.java
                                   │   └── CdcMonitoringService.java
                                   └── SyncServiceApplication.java
```

#### 변경 이유

| 변경 | 이유 |
|------|------|
| `monitoring/` → `controller/` | Controller 역할이 명확해짐 |
| `model/` → `domain/` | DDD 용어로 통일, 참고 가치 향상 |
| `CdcMonitoringService` → `service/` | Service 레이어에 위치해야 함 |

### 14.4 교육용 주석 추가

각 클래스에 `[설계 의도]`, `[프로덕션 고려사항]` 주석 추가:

```java
/**
 * CDC 동기화 서비스 (핵심 비즈니스 로직)
 *
 * [설계 의도]
 * - CDC 이벤트를 수신하여 상대 DB의 CDC 테이블에 INSERT
 * - 비즈니스 로직을 Service 레이어에 집중
 * - 데이터 접근은 JdbcTemplate 직접 사용 (프로덕션에서는 Repository 분리 권장)
 *
 * [동기화 흐름]
 * 1. Kafka Consumer가 Debezium 이벤트 수신
 * 2. 이 서비스의 syncAsisToTobe() 또는 syncTobeToAsis() 호출
 * 3. 상대 DB의 CDC 테이블에 INSERT
 * 4. DB Worker(프로시저)가 CDC 테이블 → 원본 테이블 반영 (별도 구현)
 *
 * [프로덕션 고려사항]
 * - 트랜잭션 관리: @Transactional 적용
 * - 재시도 로직: Spring Retry 또는 Kafka offset 관리
 * - 배치 처리: 대량 이벤트 시 bulk insert 고려
 */
@Service
public class CdcSyncService { ... }
```

### 14.5 최종 파일 목록 (11개)

| 패키지 | 파일 | 역할 |
|--------|------|------|
| config | CdcSimulatorConfig.java | 동적 테이블 설정 |
| config | DataSourceConfig.java | 다중 DataSource 설정 |
| config | KafkaConfig.java | Kafka Consumer 설정 |
| consumer | CdcKafkaConsumer.java | Kafka 메시지 수신 |
| controller | DashboardController.java | Thymeleaf 뷰 |
| controller | MonitoringController.java | 모니터링 REST API |
| controller | SimulatorController.java | 시뮬레이터 REST API |
| domain | CdcEvent.java | CDC 이벤트 도메인 |
| service | CdcMonitoringService.java | 인메모리 통계 |
| service | CdcSyncService.java | 핵심 동기화 로직 |
| - | SyncServiceApplication.java | Spring Boot 메인 |

### 14.6 빌드 확인

```bash
mvn clean compile
# BUILD SUCCESS
```

### 14.7 핵심 포인트

> **기억하세요**: 패키지명은 **역할**을 나타내야 합니다.
> - `monitoring` (X) - 무엇을 모니터링하는지 불명확
> - `controller` (O) - HTTP 요청 처리 역할 명확

> **기억하세요**: DDD에서 `model`보다 `domain`이 더 적절합니다.
> - `model` - 단순 데이터 구조 느낌
> - `domain` - 비즈니스 로직이 포함된 핵심 개념

---

## 15. 향후 개선사항 (프로덕션 전환 시)

> **목적**: PoC에서 프로덕션으로 전환 시 고려할 개선사항을 정리합니다.

### 15.1 에러 처리 강화

| 현재 | 개선 방향 | 우선순위 |
|------|----------|:--------:|
| try-catch + 로그 | **DLQ (Dead Letter Queue)** 도입 | 높음 |
| 실패 시 건너뛰기 | 재처리 로직 구현 | 높음 |

```java
// 권장 구현 예시
@RetryableTopic(
    attempts = "3",
    backoff = @Backoff(delay = 1000),
    dltTopicSuffix = "-dlt"
)
@KafkaListener(topics = "asis.ASIS_USER.BOOK_INFO")
public void consume(ConsumerRecord<String, String> record) { ... }
```

### 15.2 트랜잭션 관리

| 현재 | 개선 방향 |
|------|----------|
| 트랜잭션 미적용 | `@Transactional` 적용 |
| DB 작업 개별 커밋 | 논리적 단위로 트랜잭션 경계 설정 |

### 15.3 Kafka Consumer 설정

| 현재 | 개선 방향 |
|------|----------|
| `enable-auto-commit: true` | **Manual Acknowledgment** |
| 단건 처리 | **Batch Listener** 고려 |

```yaml
# 권장 설정
spring:
  kafka:
    consumer:
      enable-auto-commit: false
    listener:
      ack-mode: MANUAL
```

### 15.4 모니터링 연동

| 현재 | 개선 방향 |
|------|----------|
| 인메모리 통계 (재시작 시 초기화) | **Prometheus + Grafana** 연동 |
| 로그 기반 | **Micrometer Metrics** 적용 |

**핵심 메트릭:**
- `MilliSecondsBehindSource`: CDC 지연 시간
- `NumberOfEventsInBuffer`: 트랜잭션 버퍼 크기

### 15.5 스키마 관리

| 현재 | 개선 방향 |
|------|----------|
| JSON 직접 파싱 | **Confluent Schema Registry** + Avro/Protobuf |
| 스키마 변경 시 코드 수정 | 스키마 진화(Schema Evolution) 지원 |

### 15.6 Debezium 설정 최적화

| 항목 | 권장 설정 |
|------|----------|
| **Heartbeat** | `heartbeat.interval.ms=60000` (Oracle 세션 타임아웃 방지) |
| **CTE Query** | `log.mining.query.filter.mode=in` (Oracle 12c+, 성능 개선) |
| **Snapshot Mode** | `snapshot.mode=schema_only` (대용량 테이블 시) |

### 15.7 충돌 해결 정책 구현

| 현재 | 개선 방향 |
|------|----------|
| 문서화만 됨 | **LWW (Last Writer Wins)** 실제 구현 |
| WORKER에서 단순 덮어쓰기 | 타임스탬프 기반 충돌 감지 로직 |

### 15.8 우선순위 정리

| 순위 | 항목 | 난이도 | 영향도 |
|:----:|------|:------:|:------:|
| 1 | DLQ 도입 | 중 | 높음 |
| 2 | Manual Ack | 하 | 높음 |
| 3 | @Transactional | 하 | 중 |
| 4 | Prometheus 연동 | 중 | 중 |
| 5 | Schema Registry | 상 | 중 |
| 6 | LWW 충돌 정책 | 상 | 낮음 |

---

*작성일: 2026-01-13*
*최종 수정: 2026-01-14 (Java 소스 리팩토링, 향후 개선사항 추가)*
